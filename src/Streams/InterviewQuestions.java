package Streams;

public class InterviewQuestions {

/*	Streams Interview Questions
	⭐ Basics:
		Calculate the sum of elements in a list.
		Calculate the average of a list without using average().
		Find the minimum and maximum values from a list.
		Find the second highest and second lowest number in a list.
		Remove duplicates from a list using streams.
		Count number of elements in a list using streams.
		Convert a list of strings to uppercase/lowercase.
		Find the length of the longest string in a list.
		Concatenate a list of strings into a single comma-separated string.
		Filter out null values from a list.
		Filter even or odd numbers from a list.
		Find the square of each number in a list.
		Sort a list in ascending order.
		Sort a list in descending order.
		Count occurrences of a specific element in a list.
		⭐ Numeric Stream & Mapping:
		Convert Stream<Integer> to IntStream and sum values.
		Convert primitive stream back to boxed list.
		Generate first 10 Fibonacci numbers using Streams.
		Find product of elements using reduce.
		Difference between map() vs mapToInt() — practical example.
		⭐String Manipulation
		Count frequency of each character in a string.
		Find duplicate characters in string.
		First non-repeated character in string.
		Reverse each word in a sentence.
		Check if two strings are anagram using streams.
		Count vowels and consonants using streams.
		Remove special characters & spaces using filter.
		👥 Object List (Employee / Student)
		Find employee with highest salary.
		Find employee with second highest salary.
		Sort employees by salary then by name.
		Group employees by department.
		Count employees in each department.
		Average salary in each department.
		Partition employees by gender.
		Find oldest employee.
		Find top 3 salaried employees.
		Convert List<Employee> to Map<id, employee>.
		Handle duplicate keys in Collectors.toMap.
		Find employee names starting with “A”.
		Join employee names into single string.
		Filter employees who joined after a given year.
		Find distinct job titles from employee list.
		Find highest-paid employee per department.
		Group employees by salary range (e.g., >50k, <=50k).
		Get list of departments employees belong to (flatten nested field e.g. employee may belong to multiple departments).
		I. Employees sorted by joining date. II. Return list of employees by each department sorted by employee name descending order. (Guage whether Interviewee knows Collectors.collectingAndThen() to sort after GroupBy).
		⭐Optional + Reductions
		Safely fetch first element using findFirst().
		Use reduce() to compute factorial.
		Use reduce() to concatenate strings.
		Use reduce() to find longest string.
		⭐Collectors & Maps
		Sort map by value using streams.
		Sort map by key using streams.
		Convert Map to Stream and filter entries.
		Convert list to EnumMap based on condition.
		Return frequency map of list of elements.
		Group list into sub-lists by a predicate.
		Convert list into immutable collection using collectors.
		⭐Flattening & Transformation
		Flatten list of lists into single list.
		Flatten map values to single list.
		Convert a list of arrays into a list (flatMap).
		Convert list of strings to list of all characters.
		⭐Date & Time
		Sort employees by LocalDate field.
		Group employees by year of joining.
		Filter by date range using streams.
		⭐Statistical Problems
		Count numbers greater than threshold.
		Find numbers starting & ending with specific digits.
		Find frequency of words in list of sentences.
		Most repeated word.
		Sort words by frequency.
		Remove stop-words from a text list.
		🧠Advanced Challenging Questions
		Parallel stream vs sequential example & use case
		Custom collector to compute summary stats
		Why collect() vs toList()
		Implement infinite stream using iterate.
		Distinguish peek() vs map() with example.
		Why sorted() is a stateful intermediate operation?
		Create a stream from Iterable & Iterator.
		Handle checked exceptions inside streams.
		Reuse stream using Supplier<Stream> solution.
		Collectors.toCollection(TreeSet) example.
		Convert list into priority queue.
		Sort and preserve duplicates count.
		Drop first N elements using skip().
		Take first N elements using limit().
		Convert array of primitives to Boxed List.
		Convert String array to CSV using Streams.
		🛠 Practical Problem Solving
		Find median of list using streams.
		Find mode (most repeated number) of list using streams.
		Spiral flattening of matrix (using streams for rows).
		Validate list contains only unique elements.
		Partition into even & odd lists.
		Identify palindromes in a list.
		Remove first and last elements using streams only.
		Combine two lists and remove duplicates.
		Group by list length.
		Distinct words sorted by length.
		Find sum of digit values of each number in list.
		Convert List<String> → Map<String, Integer(length)>.
		Shuffle list using streams (trick: not directly possible — discuss).
		Stream pipeline debugging with peek
		Given an integer array, sort the array in ascending order and move all zeros (0) in the array at the end of array using streams.
}
*/
}